general:
  artifacts:
    - public

dependencies:
  pre:
    - ./ci-install-hugo.sh

test:
  override:
    - hugo --baseURL 0/home/ubuntu/blog/public

deployment:
  release:
    branch: master
    commands:
      - ./update_github_pages.sh
